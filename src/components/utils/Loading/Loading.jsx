import React from 'react';

const Loading = () => {
    return (
        <img
          className="col-12"
          src="/img/loading.gif"
          alt="loading..."
          id="img_loading"
        />
    );
};

export default Loading;